# Local model artifacts

Training writes binary `.joblib` and `.ubj` artifacts here. They are intentionally ignored because no development model is deployed automatically. The tracked benchmark report records artifact hashes, feature contracts, label provenance, hardware, and evaluation limitations.
